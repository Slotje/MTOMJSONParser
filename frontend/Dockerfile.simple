# Simplified Dockerfile - serve pre-built files
FROM nginx:alpine

# Copy pre-built Angular app
COPY dist/mtom-json-parser-frontend /usr/share/nginx/html

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
